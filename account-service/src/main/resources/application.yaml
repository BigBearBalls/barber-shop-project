spring:
  application:
    name: account-service

feign:
  clients:
    user-data-service:
      url: "${USER_SERVICE_URL}"
    user-credentials-service:
      url: "${AUTH_SERVICE_URL}"
    department-service:
      url: "${DEPARTMENT_SERVICE_URL}"