dependencies {
    annotationProcessor "org.projectlombok:lombok"
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = "eu.senla.libs"
            artifactId = "common"
            version = "1.0.0"
            from components.java
        }
    }
}
