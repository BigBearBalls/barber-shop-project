spring:
  application:
    name: gateway-service
  #  security:
  #    oauth2:
  #      resourceserver:
  #        jwt:
  #          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8082/realms/barbershop-realm}
  #          jwk-set-uri: ${KEYCLOAK_JWK_URI:http://localhost:8082/realms/barbershop-realm/protocol/openid-connect/certs}
  cloud:
    gateway:
      mvc:
        routes:
          - id: auth-service-route
            uri: http://auth-service:8080
            predicates:
              - Path=/api/v1/auth/**, /api/v1/account/**, /api/v1/permissions/**
            filters:
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          - id: booking-service-route
            uri: http://booking-service:8080
            predicates:
              - Path=/api/v1/bookings/**
            filters:
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          - id: procedure-service
            uri: http://procedure-service:8080
            predicates:
              - Path=/api/v1/procedures/**
            filters:
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          - id: user-service
            uri: http://user-service:8080
            predicates:
              - Path=/api/v1/user/**
            filters:
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          - id: calendar-service
            uri: http://calendar-service:8080
            predicates:
              - Path=/api/v1/calendars/**
            filters:
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          - id: working-day-service
            uri: http://working-day-service:8080
            predicates:
              - Path=/api/v1/working-days/**
            filters:
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

#jwt:
#  auth:
#    converter:
#      resource-id: barbershop-realm

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.springframework.cloud.gateway: DEBUG

