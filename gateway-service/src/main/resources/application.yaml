spring:
  application:
    name: gateway-service
    #  security:
    #    oauth2:
    #      resourceserver:
    #        jwt:
    #          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8082/realms/plahctoh-realm}
    #          jwk-set-uri: ${KEYCLOAK_JWK_URI:http://localhost:8082/realms/plahctoh-realm/protocol/openid-connect/certs}
    security:
      jwt:
        access-key:
          expiration-time: 10
        refresh-key:
          expiration-time: 120
  cloud:
    gateway:
      mvc:
        routes:
          # Auth Service
          - id: auth-service-route
            uri: http://auth-service:8080
            predicates:
              - Path=/auth-service/swagger-ui/**, /auth-service/v3/api-docs
              - Path=/api/v1/auth/**, /api/v1/permissions/**
            filters:
              - RewritePath=/auth-service/(?<segment>.*), /$<segment>
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          # Booking Service
          - id: booking-service-route
            uri: http://booking-service:8080
            predicates:
              - Path=/booking-service/swagger-ui.html, /booking-service/v3/api-docs
              - Path=/api/v1/bookings/**
            filters:
              - RewritePath=/booking-service/(?<segment>.*), /$<segment>
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          # Calendar Service
          - id: calendar-service-route
            uri: http://calendar-service:8080
            predicates:
              - Path=/calendar-service/swagger-ui.html, /calendar-service/v3/api-docs
              - Path=/api/v1/calendars/**
            filters:
              - RewritePath=/calendar-service/(?<segment>.*), /$<segment>
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          # User Service
          - id: user-service-route
            uri: http://user-service:8080
            predicates:
              - Path=/user-service/swagger-ui.html, /user-service/v3/api-docs
              - Path=/api/v1/user/**
            filters:
              - RewritePath=/user-service/(?<segment>.*), /$<segment>
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

          # Account Service
          - id: account-service-route
            uri: http://account-service:8080
            predicates:
              - Path=/account-service/swagger-ui.html, /account-service/v3/api-docs
              - Path=/api/v1/account/**
            filters:
              - RewritePath=/account-service/(?<segment>.*), /$<segment>
              - AddRequestHeader=X-Request-Api-Key, ${API_KEY}
              - AddRequestHeader=X-Request-Source, EXTERNAL

#jwt:
#  auth:
#    converter:
#      resource-id: plahctoh-realm

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

feign:
  clients:
    auth-service:
      url: ${AUTH_SERVICE_URL}

http-configuration:
  security:
    filters:
      get-user-id-header-filter:
        enable: false
      api-key-header-filter:
        enable: false

logging:
  level:
    org.springframework.cloud.gateway: DEBUG

